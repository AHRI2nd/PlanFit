<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Signature-level, self-declared and self-held — restricts
         HomeWidgetBackgroundReceiver below to broadcasts sent by this app
         itself. A PendingIntent's permission check resolves against the app
         that *created* it, not whichever process fires it (see
         HomeWidgetBackgroundIntent.getBroadcast in the home_widget plugin,
         which builds it from PlanFit's own Context) — so the widget's own
         checkbox tap still works untouched, while another app on the device
         independently broadcasting the same action (guessing at a to-do id
         via ?id=...) no longer can, since only PlanFit is signed with the
         certificate this permission is scoped to. -->
    <permission
        android:name="com.arisair.planfit.permission.BIND_WIDGET_BACKGROUND"
        android:protectionLevel="signature"/>
    <uses-permission android:name="com.arisair.planfit.permission.BIND_WIDGET_BACKGROUND"/>

    <!-- Calendar sync (device_calendar_plus) -->
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>
    <!-- Local notifications (flutter_local_notifications) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- App icon badge (app_badge_plus) — each covers one OEM launcher's
         own badge API; harmless no-ops on launchers that don't match. -->
    <uses-permission android:name="com.sec.android.provider.badge.permission.READ"/>
    <uses-permission android:name="com.sec.android.provider.badge.permission.WRITE"/>
    <uses-permission android:name="com.htc.launcher.permission.READ_SETTINGS"/>
    <uses-permission android:name="com.htc.launcher.permission.UPDATE_SHORTCUT"/>
    <uses-permission android:name="com.sonyericsson.home.permission.BROADCAST_BADGE"/>
    <uses-permission android:name="com.sonymobile.home.permission.PROVIDER_INSERT_BADGE"/>
    <uses-permission android:name="com.anddoes.launcher.permission.UPDATE_COUNT"/>
    <uses-permission android:name="com.majeur.launcher.permission.UPDATE_BADGE"/>
    <uses-permission android:name="com.huawei.android.launcher.permission.CHANGE_BADGE"/>
    <uses-permission android:name="com.huawei.android.launcher.permission.READ_SETTINGS"/>
    <uses-permission android:name="com.huawei.android.launcher.permission.WRITE_SETTINGS"/>

    <application
        android:label="PlanFit"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">
        <!-- Local-only app with its own explicit JSON export/restore (see
             BackupService) — opting out of Android's automatic Auto Backup
             so the personal calendar/to-do DB is never silently included in
             the user's Google account cloud backup without their knowledge. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_local_notifications: deliver scheduled notifications and
             reschedule them after a device reboot. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- HomeScreen widget (home_widget package) — see
             PlanFitWidgetProvider.kt and res/xml/home_widget_info.xml. -->
        <receiver
            android:name=".PlanFitWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/home_widget_info"/>
        </receiver>

        <!-- Interactive widget taps (the to-do checkboxes) and the periodic
             background data refresh — a background broadcast that spins up
             a headless Flutter engine to run the callback registered via
             HomeWidget.registerInteractivityCallback
             (lib/core/home_widget/home_widget_background.dart), never
             launching the app UI. See PlanFitWidgetProvider.kt's
             toggleTodoIntent/maybeRefreshWidgetData. Stays exported (the
             system needs to deliver our own PendingIntent broadcast) but
             scoped down to same-signature senders only via the permission
             declared above. -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true"
            android:permission="com.arisair.planfit.permission.BIND_WIDGET_BACKGROUND">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
